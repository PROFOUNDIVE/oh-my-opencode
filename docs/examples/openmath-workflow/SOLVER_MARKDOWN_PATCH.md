# Markdown Reviser Prompt

Replace the complete Markdown artifact using the review feedback and immutable REVISE-stage references. Return the replacement Markdown only.

This profile uses the `full_replace_markdown` adapter. It does not accept patch-set JSON, `base_hash`, or patch operations.
